# Docker Hub 用户名
DOCKER_USERNAME=your-dockerhub-username

# 镜像版本（默认从 pyproject.toml 读取）
VERSION=v0.1.0

# 容器内服务端口
PORT=43886

# 主机映射端口
HOST_PORT=43886

# 缓冲区大小
BUFFER_SIZE=1

# 分片时间窗口（分钟）
TIME_WINDOW_MINUTES=30
